# Floral Atlas Visual Prototype Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a first visual prototype that shows the approved poster-plus-index card direction for the floral atlas.

**Architecture:** Create a static HTML/CSS prototype inside the clean `flowerscards` repository. The first pass uses CSS/SVG-like botanical silhouettes and designed data blocks so the user can evaluate layout, color, typography, and mood before photo sourcing begins.

**Tech Stack:** Static HTML, CSS, local browser preview.

---

### Task 1: Static Prototype

**Files:**
- Create: `prototype/index.html`
- Create: `prototype/styles.css`

- [ ] **Step 1: Create a one-page visual prototype**

Create an HTML page with:
- one cover section;
- three Poster Cards for rose, tulip, and hydrangea;
- three Index Cards with practical floral fields;
- seasonal palette and poster microtype.

- [ ] **Step 2: Add CSS art direction**

Style the prototype with:
- archival botanical poster composition;
- muted seasonal colors;
- grain texture;
- expressive serif display typography;
- compact grotesque labels;
- readable practical cards.

- [ ] **Step 3: Verify locally**

Open the HTML file or serve the folder locally and confirm:
- the page renders;
- card text is readable;
- layout works on desktop width;
- only flower atlas files are in this repository.

- [ ] **Step 4: Commit**

Run:

```bash
git add prototype docs/superpowers/plans/2026-05-24-floral-atlas-visual-prototype-plan.md
git commit -m "Add floral atlas visual prototype"
```
